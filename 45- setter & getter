set  =>> used to "set" value 
get  =>> used to "get" or print value


Ex.

class Human{
  int age;

  set_age(int p_age){
    this.age = p_age;
  }

  get_age(){
    print("Age is: "${this.age});
  }
}

main(){
  Human h1 - new Human();
  h1.set_age(20);
  h1.get_age();
}
